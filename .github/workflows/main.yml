name: AI QA Automation Pipeline

on:
  push:
    branches:
      - main

jobs:
  test:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install Dependencies
      run: |
        pip install pytest playwright ollama
        playwright install

    - name: Run AI Test Generation
      run: |
        python app.py

    - name: Run Playwright Tests
      run: |
        pytest playwright/tests
